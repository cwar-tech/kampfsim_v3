import resolveRound
    from "../../../app/combat/resolver/resolveRound.js";

describe(
    "combat round target selection consistency",
    () => {

        test(
            "placeholder",
            () => {

                expect(true)
                    .toBe(true);
            }
        );

    }
);

